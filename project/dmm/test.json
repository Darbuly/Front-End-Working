{
	"req": {
		"API": "addClass",
		"require": [],
		"return": "${var jsonobject;\nJSON.stringify(status);}"
	},
	"resp": {
		"1": "${var jsonobject;JSON.stringify(infos);}",
		"devid": "51",
		"overtime": "0",
		"post": "${query('select max(id) as id from wei_class')[0].id}"
	}
}